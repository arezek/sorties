{% extends 'base.html.twig' %}

{% block title %}Liste Campus{% endblock %}

{% block body %}
    <h1>Campus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campus.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ campus.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('campus_index') }}">Retour à la liste</a>

    <a href="{{ path('campus_edit', {'id': campus.id}) }}">Modifier</a>

    {{ include('campus/_delete_form.html.twig') }}
{% endblock %}
