{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	<div>

		<h1>Inscription</h1>
		{{ form_start(registrationForm) }}
		{{ form_widget(registrationForm) }}
		<div class="btnSousFormulaire">
			<button type="submit" class="btn">M'inscrire</button>
		</div>
		{{ form_end(registrationForm) }}
	</div>
{% endblock %}