<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" type="image/png" sizes="20x20" href="img/logo/logoSortir.png">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        <style>
            .logoPrincipal{display: flex;margin: 10px;justify-content: left}
            .logoPrincipalimg{width: 100px; margin-left: -5px;}
            header{margin-top: 15px}
            body{background-color: #1B1B1B; margin-left: 30px; margin-right: 30px}
            li,p,h1,h2,h3,h4,h5, div{color: white}
            nav {display: flex;margin: 10px;justify-content: space-between;padding-bottom: 20px;}
            ul {list-style-type: none;margin-top : 20px;padding: 0;overflow: auto;}
            li {float: left;color: #000;text-decoration: none;padding: 5px;margin-right: 10px;margin-left: 10px;font-weight: bolder;}
            li .btnNav {display: block;color: white;text-align: center;padding: 14px 16px;text-decoration: none;border-radius: 7%;}
            li .btnNav:hover {background-color: #437d83;}
            .btnNav:link {text-decoration: none;border: none;}
        </style>
    </head>

    <header>
        <nav>
            <div class="logoPrincipal" >
                <a href="{{ path('main_index') }}">
                    <img class="logoPrincipalimg" src="{{ asset('img/logo/logoSortir.png')}}" title="Accueil" alt="Logo du site">
                </a>
            </div>
            <ul>
                <li><a class="btnNav" href="{{ path('main_villes') }}">Villes</a></li>
                <li><a class="btnNav" href="{{ path('main_campus') }}">Campus</a></li>
                <li><a class="btnNav" href="{{ path('main_index') }}">Accueil</a></li>
                <li><a class="btnNav" href="{{ path('main_profil' ,{ id : 2}) }}">Mon Profil</a></li>
                <li><a class="btnNav" href="{{ path('app_logout') }}"> Se Déconnecter</a></li>
                <li><a class="btnNav" href="{{ path('app_login') }}"> Se Connecter</a></li>
                <li><a class="btnNav" href="{{ path('app_register') }}"> S'inscrire</a></li>
                <li><a class="btnNav" href="{{ path('sorties_details') }}"> afficherSorties</a></li>
                <li><a class="btnNav" href="{{ path('sorties_cree') }}"> crée Sorties </a></li>
                <li><a class="btnNav" href="{{ path('sorties_cancel') }}"> annuler Sorties</a></li>
                <li><a class="btnNav" href="{{ path('sorties_edit') }}"> modifier Sorties</a></li>
                <li><a class="btnNav" href="{{ path('sorties_history') }}"> historiques Sorties</a></li>
                {# {% if app.user%}<a href="{{ path('app_logout') }}">Déconnexion</a>{% endif%} #}

            </ul>
        </nav>
    </header>

    <body>
        {% block body %}{% endblock %}
    </body>

    <footer >
        <h5 style="text-align: center;">© Loic-Cecyl BARBE - Robin FERRE - Abdelrezak ALLIOUCHE KERBOUA - Clément JUMELINE</h5>
    </footer>
</html>