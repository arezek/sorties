{% extends 'base.html.twig' %}

{% block title %}var Nom profil{% endblock %}

{% block body %}
<div>
    {#
    <a href = "{{  path('main_profil', {'id' : participant.id}) }}"
    src="{{ asset('img/photos/' ~ participant.photo)}}  #}
    <img src="{{ asset('img/logo/logoSortir.png') }}">
    {% for participants in participant %}

    {{  participants.pseudo }}
    {% endfor %}

</div>
{% endblock %}
