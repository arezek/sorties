{% extends 'base.html.twig' %}

{% block title %}Afficher Ville{% endblock %}

{% block body %}
    <h1>Ville</h1>
    <div>
    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ ville.nom }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ ville.codePostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ville_index') }}">Retour à la liste</a>

    <a href="{{ path('ville_edit', {'id': ville.id}) }}">Modifier</a>

    {{ include('ville/_delete_form.html.twig') }}
    </div>
{% endblock %}
