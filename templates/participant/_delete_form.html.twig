<form method="post" action="{{ path('profil_delete', {'id': participant.id}) }}" onsubmit="return confirm(' Êtes vous sûr ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.id) }}">
    <button class="btn">Delete</button>
</form>
